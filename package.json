{
  "private": true,
  "scripts": {
    "test": "jest",
    "test:e2e": "jest __tests__ --verbose --config=./jest.config.js",
    "test:e2e:focus": "NODE_ENV=dev jest --verbose --config=./jest.config.js"
  },
  "devDependencies": {
    "chalk": "^2.3.0",
    "jest": "^22.0.4",
    "prettier": "^1.9.2",
    "puppeteer": "^1.0.0",
    "rimraf": "^2.6.2"
  }
}
